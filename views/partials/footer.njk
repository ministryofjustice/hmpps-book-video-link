{% from "govuk/components/footer/macro.njk" import govukFooter %}
{% from "../components/feedbackBanner/feedbackBanner.njk" import feedbackBanner %}

{{ 
  feedbackBanner({
    feedbackBannerAdditionalClasses: feedbackBannerBackground
  })
}}

{{ 
  govukFooter({
    meta: {
      items: [
        {
          href: '/feedback-and-support',
          text: "Feedback and support"
        }
      ]
    },
    classes: "govuk-!-display-none-print qa-court-service-footer"
  }) 
}}
